# Copie para .env na raiz: cp .env.example .env
# Usado pelo docker compose (substituição ${VAR} no docker-compose.yml)

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=incident_response
POSTGRES_PORT=5432
