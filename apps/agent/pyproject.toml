[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mateus", email = "mateus.silvainfo@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
agent = "agent:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
